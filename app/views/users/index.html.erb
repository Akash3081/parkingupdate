<%= search_form_for @q do |f| %>

  <%= f.label :name_cont %>
  <%= f.search_field :name_cont, placeholder: "search user" %>

  <%= f.submit %>
<% end %>
<br>
  <% @users.each do |user| %>
    <li>
      <h4><%= link_to user.name, user %></h4>
    </li>
    <% end %><br><br><br>
   <button style="border-inline-start-color: yellow;"><a class="nav-link active" aria-current="page" href="/">Back</a></button><br>

<%= paginate @users %>